-- =============================================================================
-- 0010_fix_missing_columns.sql  ※ no-op
-- 経緯: 0004 が RtpRequestRows の ALTER TABLE で失敗していた旧インスタンス向けに
--       Transactions / Participants のカラム追加を再適用する意図で作成したが、
--       0004 修正後はゼロデプロイで 0004 がカラムを追加済みのため不要。
--       D1 は ADD COLUMN IF NOT EXISTS 非対応のため、no-op として残す。
-- =============================================================================
SELECT 1; -- no-op
